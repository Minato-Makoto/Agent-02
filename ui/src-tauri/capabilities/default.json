{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal secure capability profile",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}